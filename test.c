char main () {
    char* pointer;
    pointer = 36864; // 0x9000

    char test;
    test = 0;

    while (test < 10) {
        test = test + 1;
        char test;
    }

    // char* terminal;
    // terminal = 32767 // 0x7fff
    // *terminal = 33;

    return test;
}